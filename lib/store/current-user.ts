/**
 * Usuario "logueado" actual (mock). Cuando llegue Auth real, esto se
 * reemplaza por el profile_id del session.
 */
export const CURRENT_MEMBER_ID = "m1";
export const CURRENT_MEMBER_NAME = "Camila";
